const Filter = () => {
  return (
    <form className="d-flex my-2">
      <input
        className="my-form-control me-2"
        type="search"
        placeholder="Filter by"
        aria-label="Search"
      />
    </form>
  );
};

export default Filter;
